import type { Plugin } from 'vite';
type OramaPluginOptions = {
    analytics?: {
        enabled: boolean;
        apiKey: string;
        indexId: string;
    };
};
export declare function OramaPlugin(pluginOptions?: OramaPluginOptions): Plugin;
export {};
