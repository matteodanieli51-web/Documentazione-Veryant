import type { Results, Result } from '@orama/orama';
import { DocsDocument } from '../types';
export type SearchResultGroups = Array<[string, Array<Result<DocsDocument>>]>;
export interface PathToSectionResult {
    name: string;
    isSubSection: boolean;
}
export declare function groupSearchResultsBySection(searchResults: Results<DocsDocument>): SearchResultGroups;
export declare function removeHash(text: string): string;
export declare function pathToSection(path: string, title?: string): PathToSectionResult;
