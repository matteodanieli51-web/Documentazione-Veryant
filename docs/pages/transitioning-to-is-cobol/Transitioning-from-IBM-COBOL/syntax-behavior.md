## Syntax and Behavior

-The CHANGED flag in the EXHIBIT statement is ignored.
-To store COMP-1 (Float) and COMP-2 (Double) values using the IBM hexadecimal floating-point format, add the following entry to the configuration file:

```cobol
iscobol.floating_point_format=ibm_hfp
```
