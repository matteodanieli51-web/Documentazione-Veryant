### Output redirection

The isCOBOL RemoteCompiler service redirects all the console output (stderr and stdout) to two files named isremotecc\_err.log and isremotecc\_out.log. These files are located in the isCOBOL bin directory, which is the default directory of the service.
