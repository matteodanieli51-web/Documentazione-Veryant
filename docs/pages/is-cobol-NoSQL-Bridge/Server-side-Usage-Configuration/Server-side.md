# Server-side Usage and Configuration

NoSQL is a servlet that responds to POST requests over the HTTP protocol.

The servlet can be started out of the box using the wrapper commands installed with the product or it can be deployed in a servlet container like Tomcat.

The two approaches are described in the next chapters.
