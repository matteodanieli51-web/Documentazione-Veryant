# Reports

A Report is represented by an HTML file that the IDE generates on disk.

Once the HTML file is available, you have multiple possibilities, e.g.

- print it
- show a print preview of it
- convert it to a PDF file
- convert it to an Excel workbook

Reports are designed with the the IDE Report Designer, an interface for report design and layout that is very similar in appearance and behavior to the IDE Screen Designer.
